* watershed_algorithm.py = PURE watershed without any pathing being drawing
    FAILS when there is only 1-2 flies
    PASSES with 3+ flies
* frame_comparison_detection.py = default cv2 contouring paired with the distance calcs b/w current_bboxes
    FAILS because it CANNOT differentiate between two flies touching each other but PASSES with one fly only
* watershed_pathing.py = watershed + pathing to draw out where flies are going
    FAILS when there is only 1-2 flies
    PASSES with 3+ flies